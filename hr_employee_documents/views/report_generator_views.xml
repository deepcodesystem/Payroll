<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <!-- Report Generator Wizard Form -->
        <record id="view_hr_report_generator_form" model="ir.ui.view">
            <field name="name">hr.report.template.generator.form</field>
            <field name="model">hr.report.template.generator</field>
            <field name="arch" type="xml">
                <form string="Generate Employee Report">
                    <group>
                        <field name="employee_id" readonly="1"/>
                        <field name="template_id" domain="[('active', '=', True)]"/>
                    </group>
                    <footer>
                        <button name="generate_report" string="Download Report" type="object" class="btn-primary"/>
                        <button string="Cancel" class="btn-default" special="cancel"/>
                    </footer>
                </form>
            </field>
        </record>

        <!-- Report Generator Action -->
        <record id="action_hr_report_generator" model="ir.actions.act_window">
            <field name="name">Generate Employee Report</field>
            <field name="res_model">hr.report.template.generator</field>
            <field name="view_mode">form</field>
            <field name="target">new</field>
        </record>
    </data>
</odoo>
